<Window x:Class="NASFileWatcher.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:tb="http://www.hardcodet.net/taskbar"
        Title="NAS File Watcher" 
        Height="0" 
        Width="0"
        WindowStyle="None"
        ShowInTaskbar="False"
        Visibility="Hidden"
        ResizeMode="NoResize">
    <Grid>
        <tb:TaskbarIcon x:Name="TrayIcon"
                        IconSource="/Assets/app.ico"
                        ToolTipText="NAS 檔案監控 - 運行中"
                        TrayLeftMouseDown="TrayIcon_TrayLeftMouseDown">
            <tb:TaskbarIcon.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="📋 查看最近通知" Click="ViewRecentNotifications_Click" />
                    <MenuItem Header="⚙️ 設定" Click="Settings_Click" />
                    <Separator/>
                    <MenuItem Header="📄 開啟日誌檔" Click="OpenLog_Click"/>
                    <MenuItem x:Name="PauseMenuItem" Header="⏸️ 暫停監控" Click="PauseResume_Click"/>
                    <Separator/>
                    <MenuItem Header="❌ 結束程式" Click="Exit_Click" />
                </ContextMenu>
            </tb:TaskbarIcon.ContextMenu>
        </tb:TaskbarIcon>
    </Grid>
</Window>
